# Archive-capable mainnet RPC (Alchemy/Infura). Must support eth_getBalance at historical blocks.
RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY

# Local Anvil endpoint (started with `npm run anvil`). Defaults to this if unset.
ANVIL_URL=http://127.0.0.1:8545
